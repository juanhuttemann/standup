# Provider settings (required unless offline: true in config/config.yaml)
OPENAI_BASE_URL=
OPENAI_MODEL=
# Speech settings (required for `speak -o` only; the model must support audio output)
OPENAI_SPEECH_MODEL=
OPENAI_SPEECH_VOICE=
